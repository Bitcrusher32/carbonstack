# CarbonStack v0.6.34 Release-Candidate Package Surface Audit v0

Status: release-candidate package surface audit checkpoint before manual v0.7.0 Gitea release insertion.

Scope:

- audit the v0.7.0 package surface after v0.6.33 package-script/rehearsal work;
- verify staged release assets exist and checksum correctly;
- verify package checksums from fresh extraction;
- verify expected multi-repo package shape;
- verify carbonstack-os remains excluded from the runnable package;
- verify registry/generated-reference surfaces are present;
- verify forbidden generated/private/build artifacts are absent;
- prepare a final local release-artifacts folder for manual Gitea insertion after this audit checkpoint is committed and pushed.

This checkpoint does not create a tag, upload assets, publish a release page, claim production security, claim deployment readiness, claim hostile-server safety, or claim CarbonStackOS readiness.

## Release-candidate package shape

Included runnable package roots:

- carbonstack
- carbonstack-comms
- carbonstack-cypher
- release

Excluded runnable package root:

- carbonstack-os

carbonstack-os remains future constrained-appliance OS work only.

## Release-candidate assets

The intended manual Gitea upload asset set is:

- carbonstack-v0.7.0-cumulative-pre-alpha-engineering-boundary-pre-release.tgz
- carbonstack-v0.7.0-release-manifest.json
- carbonstack-v0.7.0-package-checksums.txt
- carbonstack-v0.7.0-asset-checksums.txt
- carbonstack-v0.7.0-validation-freeze.md
- v0.7.0-testing-runbook.md
- v0.7.0-release-notes.md
- LICENSE

Gitea default Source Code ZIP/TAR.GZ archives are not the intended multi-repo validation package.

## Validation posture

Primary validation:

    go run . --profile verify-checksums --root <package-root>
    go run . --profile full-validate-release --root <package-root> --clean-generated

Compatibility validation:

    go run . --profile full --root <package-root> --clean-generated

full-validate-release remains the release-facing name for the existing release package validation ladder.

Current behavior:

    full-validate-release = release-snapshot + local-cypher

release-snapshot already calls core.

## Audit checks

v0.6.34A checks:

- script syntax for stage/rehearse v0.7.0 helpers;
- generated command reference deterministic check at entries=89;
- runner tests;
- registry missing-nonclaims scan;
- package staging;
- package archive creation;
- staged release asset existence;
- staged asset checksum verification;
- fresh extraction;
- package checksum verification from fresh extraction;
- full-validate-release from fresh extraction through the rehearsal helper;
- final package artifact scan;
- manifest target/version/title/nonclaims;
- carbonstack-os exclusion;
- release/source archive warning;
- stale v0.6.0/v0.5 release asset wording scan.

## Hard nonclaims

v0.6.34A and the generated manual release-artifact folder do not claim production secure messaging, production E2EE, local-backbone, hostile-server safety, malicious relay safety, metadata privacy, verified identity, secure enrollment, production vault/key storage, backup/restore, PQ/hybrid security, quantum-safe messaging, Android readiness, CarbonStackOS readiness, deployment readiness, public ingress safety, audit/certification, or general-public usability.

## Blunder note

The first v0.6.34A audit script passed the pre-doc package audit, but failed while writing docs/233 because the audit helper left the working directory inside the extracted package root. The failure was not a package failure and not paste truncation. The continuation script writes docs/233 from the real carbonstack repo root before final committed-head rehearsal.
