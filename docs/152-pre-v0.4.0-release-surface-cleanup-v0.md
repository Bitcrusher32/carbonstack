# CarbonStack v0.3.33 Pre-v0.4.0 Release-Surface Cleanup

Status: v0 public-surface cleanup checkpoint
Scope: late v0.3.x -> v0.4.0 release runway
Primary environment: WSL Debian
Generated: 2026-06-03 08:41:42 -0400

## 1. Purpose

This document records the v0.3.33 release-surface cleanup rung.

v0.3.32 added the first negative-path check to `local-cypher`. v0.3.33 does not add validation features. It cleans the public surface before the pre-v0.4.0 release runway.

## 2. Current repo heads before this docs commit

    carbonstack        cff3ab4 docs: record local-cypher negative protocol validation
    carbonstack-comms  012c8bf scripts: support source snapshot artifact guard
    carbonstack-cypher 9ab994c docs: point to local operator runbook
    carbonstack-os     1bbbe52 docs: clarify CarbonStackOS target direction

## 3. Recon findings

The release-surface scout found:

    README had duplicate Current testing path sections.
    README still described near-term v0.3.x using older v0.3.20-era language.
    roadmap still carried older v0.2.x/v0.3.0 scaffolding ahead of newer current-state material.
    docs/README did not yet surface v0.3.32/local-cypher as current mainline validation state.
    validation itself was healthy: local-cypher, doctor, and core --clean-generated passed.
    final clean status remained clean.

## 4. Cleanup performed

Updated files:

    README.md
    roadmap/ROADMAP.md
    docs/README.md
    docs/152-pre-v0.4.0-release-surface-cleanup-v0.md

Main cleanup:

    remove duplicated current testing path wording
    keep v0.3.20 as current public testing release
    surface v0.3.32 as current mainline validation state
    frame v0.4.0 as broad local deployability pre-release
    preserve strict nonclaims
    reserve local-backbone for later whole-stack validation
    preserve CarbonStack Relay Space as the future official term

## 5. v0.4.0 framing

v0.4.0 should be framed as:

    broad local deployability pre-release
    milestone / research-and-development release
    WSL Debian runner-backed validation surface
    not public-user-ready
    not application-use-ready
    not production secure
    not hostile-server-certified
    not Android or CarbonStackOS
    not a mature Comms release

## 6. Current concrete validation surface

Current mainline commands:

    cd tools/carbonstack-validate
    go run . --profile local-cypher
    go run . --profile doctor
    go run . --profile core --clean-generated

Current public testing release remains:

    v0.3.20 runner-backed testing release

## 7. Current nonclaims

This cleanup does not claim:

    production readiness
    production E2EE
    hostile-server safety
    metadata privacy
    deployment readiness
    systemd readiness
    cloudflared readiness
    public ingress safety
    Android readiness
    CarbonStackOS readiness
    external audit
    certification

## 8. Recommended next rung

Recommended next rung:

    v0.3.34 release checklist and package validation rehearsal

Focus:

    package/release checklist
    release-snapshot/checksum verification
    fresh extraction validation
    final v0.4.0 release-note draft prep
    no new feature work unless release-surface validation exposes a blocker
