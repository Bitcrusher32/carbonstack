# v0.5.50 ACK_AFTER_JOIN smoke evidence and validation-profile preflight

Status: live dev/pre-alpha ACK_AFTER_JOIN smoke evidence / validation-profile preflight
Phase: v0.5.x Relay Space + OpenMLS narrow smoke-proof stabilization
Scope: ACK_AFTER_JOIN evidence, DB ack evidence, validation-profile readiness boundary
Date: 2026-06-09 local session

## 1. Purpose

v0.5.50 records the successful fresh ACK_AFTER_JOIN=1 narrow join smoke proof.

This checkpoint records evidence and validation-profile implications.

It does not add:

    validation profile;
    runner profile;
    public README promotion;
    new Comms app command;
    new script;
    command registry entry;
    Cypher route;
    trust.json mutation;
    identity-candidates.json mutation;
    candidate observation;
    local-backbone claim;
    hostile-server harness.

## 2. Relationship to v0.5.49

v0.5.49 recorded the first successful live no-ack narrow join smoke proof.

v0.5.50 records the complementary explicit ACK_AFTER_JOIN=1 smoke proof.

Together, these prove the current dev/pre-alpha command sequence can express:

    KeyPackage submit;
    add-member + Welcome submit;
    Welcome join without ack by default;
    Welcome join with scoped ack only when explicitly requested after join success.

This is still not a validation profile.

This is still not local-backbone.

## 3. Preconditions used

The ACK_AFTER_JOIN run used fresh generated state:

    fresh local Cypher database at /tmp/carbonstack-v0550-cypher.db;
    Cypher dev mode with dev invite enabled;
    fresh Alice/Bob Comms state files under /tmp;
    fresh Alice/Bob Cypher accounts;
    fresh Alice/Bob Cypher devices;
    Relay Space ID v0550-ack-smoke-relay-space;
    both devices registered as active Relay Space routing members;
    fresh OpenMLS sidecar labels:
        carbonstack-v0550-alice-device;
        carbonstack-v0550-bob-device;
    fresh OpenMLS conversation label:
        carbonstack-v0550-conversation;
    ACK_AFTER_JOIN=1.

## 4. Successful sequence

The successful ACK_AFTER_JOIN smoke sequence completed:

    Bob openmls-relay-keypackage-submit-dev;
    Alice openmls-relay-add-member-dev;
    Bob openmls-relay-join-dev with ACK_AFTER_JOIN=1.

## 5. Step 1 evidence: KeyPackage submit

Observed:

    command: openmls-relay-keypackage-submit-dev
    status: sent
    sidecar_command: public-bundle-export
    content_type: carbonstack.mls.keypackage.v0
    protocol_version: carbonstack-openmls-sidecar-v0
    delivery_state: queued
    payload_size_bytes: 302

This proves only that Bob exported a dev OpenMLS KeyPackage artifact and submitted it through the Relay Space envelope path to Alice.

The KeyPackage envelope remained queued after the run.

## 6. Step 2 evidence: add-member and Welcome submit

Observed:

    command: openmls-relay-add-member-dev
    status: welcome_created_and_sent
    sidecar_command: conversation-add-member
    keypackage_acked: false
    welcome_acked: false
    member_added: true
    welcome_artifact_written: true
    group_reloadable: true
    member_count_before: 1
    member_count_after: 2
    epoch_before: GroupEpoch(0)
    epoch_after: GroupEpoch(1)
    welcome_delivery_state: queued
    welcome_payload_size_bytes: 893

This proves only that Alice consumed the queued KeyPackage artifact, ran the dev sidecar add-member path, produced a Welcome artifact, and submitted it through the Relay Space envelope path to Bob.

The add-member step does not ack the KeyPackage.

The add-member step does not ack the Welcome.

## 7. Step 3 evidence: ACK_AFTER_JOIN Welcome join

Observed:

    command: openmls-relay-join-dev
    status: joined
    sidecar_command: conversation-join
    ack_requested: true
    welcome_acked: true
    ack_delivery_state: acknowledged
    joined: true
    group_reloadable: true
    member_count: 2
    epoch: GroupEpoch(1)

Observed ack fields:

    ack_envelope_id: 4da140d8-19a7-442e-9c30-31129ba15331
    acknowledged_at: 2026-06-09T13:44:45Z

This proves only that Bob consumed the queued Welcome artifact, the dev sidecar conversation-join path reported a joined/reloadable group, and the scoped Welcome ack occurred after join success because ACK_AFTER_JOIN=1 was explicitly set.

## 8. DB evidence

The fresh v0.5.50 Cypher DB reported:

    accounts: 2
    devices: 2
    relay_spaces: 1
    relay_space_members: 2
    envelopes: 2
    envelope_acks: 1

Envelope evidence:

    KeyPackage envelope:
        content_type: carbonstack.mls.keypackage.v0
        delivery_state: queued
        payload_size_bytes: 302

    Welcome envelope:
        content_type: carbonstack.mls.welcome.v0
        delivery_state: acknowledged
        payload_size_bytes: 893

Ack evidence:

    one envelope_acks row existed;
    the ack referenced the Welcome envelope;
    the ack recipient was Bob's device;
    acknowledged_at was populated.

This is the desired scoped ack result for ACK_AFTER_JOIN=1.

## 9. Trust/candidate nonmutation observation

Before and after the run, the checked local state directory showed absence of:

    trust.json;
    trust-events.jsonl;
    identity-candidates.json.

This supports the expected no trust/candidate mutation behavior for this run.

It is not a proof of general safety.

## 10. Ack boundary now evidenced

The combined v0.5.49 and v0.5.50 evidence supports:

    Welcome is not acked by default.
    Welcome is acked when ACK_AFTER_JOIN=1.
    The ACK_AFTER_JOIN path reports join success and then scoped ack success.
    KeyPackage is not acked by this smoke script.
    Ack is local-processing/delivery state, not identity verification.

## 11. Validation-profile implication

The project is now closer to a narrow local-dev validation profile.

However, validation-profile implementation remains blocked until a dedicated profile design defines:

    generated-state ownership;
    fresh sidecar label/state strategy;
    fresh Cypher DB strategy;
    cleanup boundaries;
    safe refusal if state already exists;
    exact output assertions;
    failure-path checks;
    trust/candidate nonmutation checks;
    registry/front README checks;
    exact profile claims and nonclaims.

## 12. Nonclaims

This evidence does not claim:

    local-backbone;
    production secure messaging;
    hostile-server safety;
    metadata privacy;
    verified identity;
    secure enrollment;
    mature provider live-flow;
    public CLI readiness;
    deployment readiness;
    audit;
    certification.

## 13. Recommended next rung

Recommended next rung:

    validation-profile design only.

The profile should probably start as a narrow local-dev profile that owns its generated state and proves only the v0.5.49/v0.5.50 smoke sequence boundaries.

Do not implement it until generated-state and cleanup ownership are exact.
