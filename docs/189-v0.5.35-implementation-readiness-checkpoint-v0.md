# v0.5.35 implementation-readiness checkpoint

Status: cleanup / implementation-readiness checkpoint
Phase: v0.5.x post-local-backbone-go-no-go / pre-first-narrow-implementation
Scope: public wording cleanup, roadmap alignment, first implementation rung naming, CLI/registry deferral, continuity procedure
Date: 2026-06-08 local session

## 1. Purpose

v0.5.35 checks whether CarbonStack is clean enough to move from the v0.5.30-v0.5.34 planning arc toward a first narrow implementation rung.

This is a cleanup and readiness checkpoint.

v0.5.35 does not implement:

    Relay Space schema;
    Relay Space API routes;
    Relay Space join flow;
    provider live-flow wiring;
    validation profile changes;
    CLI/registry entries;
    local-backbone;
    destructive reset/recovery;
    backup/restore;
    re-enrollment mutation;
    vault;
    PQ/hybrid crypto.

Decision:

    update stale public roadmap/docs wording from the v0.5.17 era to the current v0.5.35 implementation-readiness state;
    keep the current public release as v0.5.0;
    keep v0.5.34 as conditional GO for first narrow implementation planning only;
    name the next implementation target as Cypher Relay Space schema/API substrate;
    defer local-backbone profile, broad provider live-flow, CLI/registry, and full local-backbone;
    require latest LogDoc resend and planning-doc scout before implementation.

## 2. Recon findings

The v0.5.35 recon found:

    repo heads were clean at the start;
    docs/README.md still described the current mainline checkpoint as v0.5.0/v0.5.1-era baseline;
    roadmap/ROADMAP.md still described current state after v0.5.17 and contained stale statements that candidate identity storage and mapped mismatch implementation did not exist;
    registry/commands.v0.yaml remains intentionally provisional and should not be expanded yet;
    component docs already carry v0.5.31-v0.5.34 boundaries;
    public-surface PII/local path/hostname hygiene scan did not produce hits;
    validation passed;
    final post-recon repo status was clean across all four repos.

## 3. Cleanup performed

v0.5.35 updates:

    docs/README.md current checkpoint and v0.5.x direction;
    docs/README.md latest-doc index with this v0.5.35 checkpoint;
    roadmap/ROADMAP.md to current v0.5.35 planning state.

v0.5.35 does not update:

    registry/commands.v0.yaml;
    validation runner code;
    component implementation code;
    Cypher schema/API;
    Comms runtime flows.

## 4. Current public release

Current public Gitea-source-of-truth release remains:

    v0.5.0 Runtime and Registry Validation Minor Epoch Pre-Release.

v0.5.35 is mainline development state, not a new public release package.

## 5. Current mainline state

Current mainline state after v0.5.35:

    v0.5.30 local-backbone blocker reassessment selected Relay Space recon/planning;
    v0.5.31 defined practical Relay Space addressing, invite, join, and qualified-member semantics;
    v0.5.32 defined provider/OpenMLS live-flow boundaries and deferred broad live-flow;
    v0.5.33 defined validation-profile claim and cleanup boundaries;
    v0.5.34 granted conditional GO for first narrow implementation planning;
    v0.5.35 cleans stale public wording and confirms implementation-readiness procedure.

## 6. First implementation target

Preferred first implementation target:

    Cypher Relay Space schema/API substrate.

This may include later narrow rungs for:

    relay_spaces table;
    relay_space_invites table;
    relay_space_members table;
    relay_space_id-scoped envelope support or migration path;
    create/get/list Relay Space APIs;
    invite create/claim APIs;
    routing member registration/list APIs;
    routing-only tests;
    no-trust-authority tests.

This should still be split if a schema-only first rung is safer.

## 7. What remains deferred

Deferred:

    full local-backbone;
    Comms broad provider live-flow;
    provider event broad import;
    automatic candidate promotion;
    trust.json mutation from provider observation;
    Relay Space membership as trust;
    OpenMLS/provider membership as local verification;
    local-backbone validation profile;
    CLI/registry exposure;
    destructive reset/recovery/re-enrollment helpers;
    vault;
    PQ/hybrid;
    hostile-server harnesses;
    deployability claims.

## 8. CLI / registry decision

Do not add CLI or registry entries in v0.5.35.

Reason:

    implementation does not exist yet;
    validation profile does not exist yet;
    commands would overstate support;
    registry exposure should follow actual useful dev surfaces.

Existing registry entries remain a provisional navigation and claim-boundary artifact.

## 9. Validation decision

Do not add a local-backbone validation profile in v0.5.35.

Reason:

    Relay Space schema/API substrate does not exist yet;
    provider live-flow remains deferred;
    full local-backbone is not implemented;
    profile claims would still overstate the system.

Future validation should be introduced only after concrete substrate exists and can state exactly what it proves and does not prove.

## 10. Implementation continuity procedure

Before the first implementation rung after this planning arc:

    user must resend the latest LogDoc as a direct continuity reference;
    assistant must scout the controlling planning docs again;
    assistant must confirm first-rung scope before generating code patches;
    implementation must remain narrow and claim-safe.

This procedure is required because the v0.5.30-v0.5.35 planning context is dense and can rot with procedure.

## 11. Recommended next rung

Recommended next rung:

    v0.5.36+ first narrow Cypher Relay Space schema/API substrate implementation planning and/or implementation, only after latest LogDoc resend and planning-doc scout.

First implementation should not be called full local-backbone.

Safer naming:

    Cypher Relay Space substrate;
    Relay Space schema/API substrate;
    first narrow Relay Space routing/storage substrate.

## 12. Nonclaims

v0.5.35 does not claim:

    full local-backbone readiness;
    local-backbone implementation;
    local-backbone validation profile;
    Relay Space implementation;
    Relay Space schema/API;
    provider live-flow implementation;
    OpenMLS join automation;
    Comms Relay Space client support;
    CLI/registry update;
    release-package local-backbone support;
    mature verification ceremony;
    verified provider identity import;
    trust.json mutation;
    key replacement;
    metadata privacy;
    hostile-server safety;
    production secure messaging;
    destructive reset/recovery;
    backup/restore support;
    re-enrollment implementation;
    vault security;
    PQ/hybrid security;
    external audit;
    certification.
