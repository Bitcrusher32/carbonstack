# v0.3.4 Public Release Asset Verification v0

Status: verification record
Phase: v0.3.5 backbone maturation
Release verified: CarbonStack v0.3.4 testing bug hotfix

## 1. Purpose

This document records verification of the public v0.3.4 release assets after upload to Gitea.

v0.3.4 fixed the source-snapshot self-test issue found at v0.3.3. This v0.3.5 check verifies the uploaded public release assets, not the local staging directory.

The goal is to prove that the public v0.3.4 release package can be downloaded, checksum-verified, extracted, and used to run the public OpenMLS backbone self-test path from no-Git source snapshots.

## 2. Release tested

Release:

    CarbonStack v0.3.4 testing bug hotfix

Release page:

    https://git.bitcrusher32.win/bitcrusher32/carbonstack/releases/tag/v0.3.4

Release class:

    pre-alpha / experimental

Primary artifact:

    Cypher + Comms OpenMLS relay backbone

## 3. Assets checked

The verification downloaded the public v0.3.4 release assets:

    carbonstack-v0.3.4-source-snapshot.zip
    carbonstack-comms-v0.3.4-source-snapshot.zip
    carbonstack-cypher-v0.3.4-source-snapshot.zip
    carbonstack-v0.3.4-release-manifest.json
    carbonstack-v0.3.4-validation-freeze.md
    carbonstack-v0.3.4-checksums.txt
    v0.3.4-testing-bug-hotfix.md
    LICENSE

## 4. Verification results

Public v0.3.4 asset download:

    PASS

Checksum-covered asset verification:

    PASS

Source snapshot extraction:

    PASS

Pre-test forbidden shipped-artifact scan:

    PASS

No forbidden generated/private/build artifacts were found before tests were run.

Public self-test from extracted no-Git source snapshot:

    PASS

Public self-test `-Full` from extracted no-Git source snapshot:

    PASS

CarbonStack `validate-local.ps1` from extracted no-Git source snapshot:

    PASS

## 5. Post-test artifact scan

After validation, generated artifacts were present under the clean-run root.

Observed generated categories included:

    target/
    .carbonstack-openmls-sidecar-state/
    provider-storage.json
    signer.json
    .phase1-validation-temp/cypher-validation.db
    Rust build executables

These are post-validation outputs.

They were not present in the pre-test shipped-artifact scan.

They must not be committed or included in future release source snapshots.

## 6. Interpretation

v0.3.5 verifies that the uploaded public v0.3.4 release assets carry the source-snapshot-safe testing fix.

The v0.3.3 source-snapshot self-test bug is not reproduced by the uploaded v0.3.4 release package.

The public v0.3.4 release can be treated as the current recommended testing release for the experimental backbone.

## 7. What this validates

This validates that:

    the public v0.3.4 assets can be downloaded;
    checksum-covered assets match;
    source snapshots extract cleanly;
    shipped source snapshots do not contain obvious generated/private/build artifacts before tests;
    the public OpenMLS backbone self-test runs from no-Git source snapshots;
    the public OpenMLS backbone self-test `-Full` runs from no-Git source snapshots;
    CarbonStack validate-local runs from no-Git source snapshots.

## 8. What this does not validate

This does not validate:

    production readiness;
    production E2EE;
    hostile-server safety;
    metadata privacy;
    Android readiness;
    secure vault/storage;
    Linux portability;
    Debian deployability;
    external audit;
    certification.

## 9. Next work

Continue v0.3.x backbone maturation.

Recommended next rungs:

    self-test/runbook UX cleanup;
    release testing instructions;
    portability assumptions recon;
    Debian/Linux recon after Windows release basics are stable;
    low-level deploy configuration and operator model.

Do not start runtime Comms OpenMLS send/inbox UX until release basics and portability assumptions are cleaner.
