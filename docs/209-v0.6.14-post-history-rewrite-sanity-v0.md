# CarbonStack v0.6.14 Post-History-Rewrite Sanity Checkpoint v0

Status: no-runtime-change sanity checkpoint.

Scope:

- record the hard sanity recon after the heavy v0.6.13 generated-reference and public-history rewrite checkpoint;
- preserve the release/tag sanity result after the v0.6.0 tag target changed during author-history hygiene;
- record generated command-reference determinism and registry/reference consistency;
- record public main/tag identity hygiene;
- add a recurring generated-reference freshness guard;
- preserve a private local git-bundle freeze before deleting local-only backup branches.

Non-scope:

- no runtime behavior change;
- no Comms behavior change;
- no Cypher behavior change;
- no CarbonStackOS behavior change;
- no release retag as a new public release;
- no release asset rebuild;
- no Unix man pages;
- no generated replacement for COMMAND_BOUNDARY_TABLE.v0.md.

## Source-of-truth heads after v0.6.13 history rewrite

    carbonstack        c5bc95c registry: add generated command reference
    carbonstack-comms  53779d5 test: cover message wrapper residual edge cases
    carbonstack-cypher d18a564 chore: restore validated Go module floor
    carbonstack-os     1bbbe52 docs: clarify CarbonStackOS target direction

All four repos were aligned with origin/main during the v0.6.14 recon.

## Public identity hygiene result

The v0.6.14 recon reported public main/tag local host-derived identity counts of zero for all four repos:

    carbonstack        0
    carbonstack-comms  0
    carbonstack-cypher 0
    carbonstack-os     0

Remote pre-rewrite backup branches were absent.

CarbonStackOS still had local-only backup branches before this checkpoint. Before deleting those local branches, the operator created a private umbrella freeze tarball containing git bundles for all four repos.

Freeze artifact name:

    CarbonStack_Umbrella_v0.6.12FREEZE.tar.gz

Boundary:

    This freeze is a private rollback/history safety artifact, not a public release package.

## Release/tag sanity result

The v0.6.14 recon confirmed:

    local v0.6.0 tag  = 70d4318 scripts: add v0.6.0 package rehearsal
    remote v0.6.0 tag = 70d4318
    current main      = c5bc95c registry: add generated command reference

Gitea release API for v0.6.0 remained reachable.

Release page summary from the recon:

    tag_name: v0.6.0
    target_commitish: main
    name: CarbonStack v0.6.0 State/UX Boundary Validation Pre-Release
    prerelease: true
    asset_count: 8

The attached release asset checksum file verified all eight attached assets.

The downloaded attached v0.6.0 package was extracted and validated with:

    go run . --profile verify-checksums --root <downloaded package root>
    go run . --profile full --root <downloaded package root> --clean-generated

Both package validation steps passed.

Important boundary:

    Attached v0.6.0 assets remain the intended runnable release surface.
    Gitea auto source archives are still not the intended runnable multi-repo package.

## Generated command-reference sanity result

The generated reference remained current and deterministic:

    python3 tools/registry/render-command-reference.py --check

The v0.6.14 recon confirmed:

    registry_ids=79
    reference_ids=79
    unique_registry_ids=79
    unique_reference_ids=79
    missing_from_reference=[]
    extra_in_reference=[]
    duplicate_registry_ids=[]
    duplicate_reference_ids=[]

The generated reference preserved:

- generated dev/operator command reference status;
- registry presence is classification, not promotion;
- not general-public UX documentation;
- not production security;
- not a man-page set;
- full, release-snapshot, and integrated-runtime-dev remain distinct;
- historical scripts are grouped under Historical scripts and smoke helpers;
- internal sidecar and Cypher API sections remain explicit.

## Stale pre-rewrite SHA scan result

The v0.6.14 recon found tracked historical references to pre-rewrite SHAs.

Most were inside the sanitized historical PRIME LogDoc archive. These are intentionally historical and should not be mass-edited without a separate archival policy.

Three normal historical docs also referenced old Cypher evidence SHAs:

    docs/194-v0.5.49-live-narrow-join-smoke-proof-evidence-v0.md
    docs/197-v0.5.56-live-negative-path-design-matrix-v0.md
    docs/198-v0.5.60-registry-manual-boundary-v0.md

Decision:

    Do not rewrite old evidence docs in this checkpoint.
    Treat those references as pre-v0.6.13 historical evidence anchors.
    Current Gitea source-of-truth heads and tags are the post-v0.6.13 values recorded in the current LogDoc.

## Validation carried forward

The v0.6.14 recon passed:

    generated reference deterministic check
    generated reference entry count check
    generated reference boundary header check
    generated reference script grouping check
    generated reference internal/API section check
    registry/reference exact ID consistency
    no generated man pages guard
    Gitea release API asset listing
    attached release asset checksum verification
    downloaded public package verify-checksums
    downloaded public package full --clean-generated
    registry-lookup --missing-nonclaims
    registry lookup spot checks
    runner go test ./...
    runner doctor
    integrated-runtime-dev --clean-generated

## v0.6.14 source change

This checkpoint adds a recurring generated-reference freshness test to the runner test suite.

Purpose:

    If registry/commands.v0.yaml changes but registry/COMMAND_REFERENCE.v0.md is not regenerated, go test ./... in tools/carbonstack-validate should fail.

Boundary:

    This is a validation/docs guard only.
    It does not change command behavior.
    It does not make generated docs public UX.
    It does not generate man pages.

## Next rung

After v0.6.14, the next intended engineering direction is same-state integrated proof recon.

Target concept:

    strengthen integrated-runtime-dev beyond sequential profile composition by studying whether onboarding plus normal message flow can be proven in one coherent state story.

Do not jump directly to:

- vault implementation;
- PQ/hybrid implementation;
- adversarial relay harness;
- man-page generation;
- full/release-snapshot mutation;
- production security claims.
